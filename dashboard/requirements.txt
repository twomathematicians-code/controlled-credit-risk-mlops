# Slim dependencies for the Streamlit dashboard only.
# Use this file when deploying to Streamlit Community Cloud so the environment
# builds fast. It intentionally omits xgboost (the dashboard's in-memory demo
# model is a logistic regression; explainability falls back gracefully).
streamlit>=1.30
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10
matplotlib>=3.7
shap>=0.43
mlflow>=2.8
pyarrow>=12.0
pydantic>=2.0
pyyaml>=6.0
